<h1>My Task</h1>
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<input type="checkbox"><%= @task.completed %> <br>
<%= link_to 'Edit', edit_task_path(@task.id) %> <br>
<%= link_to 'Back to the list', tasks_path %> <br>
<%= link_to '🗑', task_path(@task), method: :delete, data: { confirm: 'Are you sure?' } %>

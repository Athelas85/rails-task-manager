<h1>My Tasks</h1>
  <% @tasks.each do |task| %>
    <input type="checkbox">
    <label for="task"><%= link_to task.title, task_path(task) %></label><br>
  <% end %>
<%= link_to 'Add a new task', tasks_new_path %>
